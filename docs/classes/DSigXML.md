DSigXML CIDSigXML цифровая подпись XML ФГИС Зерно КриптоПро сертификат SetCertificate Sign GetLastError цифровая подпись electronic signature DSigXML класс EME-L

### Класс DSigXML — цифровая подпись XML-документов в языке EME-L

Класс `DSigXML` в языке EME-L предназначен для создания цифровой подписи XML-документов с использованием внешнего исполняемого файла на C#. Класс применяется для формирования электронной подписи XML-запросов в системе ФГИС Зерно.

Для работы класса `DSigXML` в языке EME-L требуются: установленный КриптоПро CSP 5.0, .NET 6.0 и действующий сертификат с закрытым ключом в личном хранилище сертификатов Windows (хранилище «MY»). Внешний C#-процесс обменивается данными с EME-L через временные XML-файлы — он читает неподписанный XML и записывает подписанный результат или файл ошибки.

Класс зарегистрирован в `p_System.cpp` (`REGISTRY_EMEL_CLASS(DSigXML, ...)`) под меткой `#ZIA 11.07.2023`. C++-имя класса — `CIDSigXML`.

#### Создание объекта

Объект класса `DSigXML` создаётся через `Object()` с опциональным указанием серийного номера сертификата:

```EME-L
objDSig = Object("DSigXML");
objDSig = Object("DSigXML", "123ABCDEF");
```

Конструктор без аргументов создаёт объект без установленного сертификата. Каталог для работы с внешним исполняемым файлом определяется из параметра модуля «Предпочтения.УТМ.СДИЗ.Путь к каталогу генератора подписи для ФГИС Зерно».

Конструктор с серийным номером выполняет автоматический поиск сертификата в хранилище «MY». Если сертификат не найден, объект создаётся, но метод `GetLastError` вернёт сообщение об ошибке. При успешном поиске сертификат устанавливается для последующего подписания.

#### Методы

| Метод | Аргументы | Возвращает | Описание |
|-------|-----------|------------|----------|
| `SetCertificate(arg0)` | arg0: String (серийный номер сертификата) | Boolean | Ищет сертификат в хранилище «MY» по серийному номеру без учёта регистра. TRUE — сертификат найден и установлен, FALSE — не найден или хранилище недоступно. |
| `Sign(arg0)` | arg0: String (XML-содержимое) | String | Формирует цифровую подпись XML через внешний C#-процесс. Возвращает подписанный XML-документ. При ошибке — пустая строка. |
| `GetLastError()` | — | String | Текст последней ошибки, возникшей при работе с сертификатом или подписании XML. Пустая строка при отсутствии ошибок. |

#### SetCertificate — установка сертификата в системе EME.WMS

Метод `SetCertificate` выполняет поиск сертификата в системном хранилище Windows «MY» по серийному номеру без учёта регистра символов. При возврате FALSE сообщение об ошибке доступно через `GetLastError`. Если параметр не указан или пустой, поиск в хранилище выполняется, но совпадения с пустой строкой не находится — метод возвращает FALSE.

Перед вызовом `Sign` сертификат должен быть установлен — либо через конструктор с аргументом, либо методом `SetCertificate`. Если подписать XML при отсутствии установленного сертификата, `Sign` возвращает пустую строку, а `GetLastError` возвращает «Сертификат не был найден».

#### Sign — формирование цифровой подписи в языке EME-L

Метод `Sign` создаёт цифровую подпись XML-документа с использованием установленного сертификата. Обмен данными с внешним C#-процессом выполняется через временные XML-файлы: XML-содержимое сохраняется в файл `tobesignedxml.xml`, а результат читается из `signedxml.xml`. При ошибке во внешнем процессе создаётся файл журнала ошибок, содержимое которого доступно через `GetLastError`.

#### Примеры

Установка сертификата и подписание XML. Реальный пример из производственного класса `SDIZ` (метод `SigningOfXML`):

```EME-L
'Создать объект DSigXML и установить сертификат по серийному номеру'
XMLSigner = Object("DSigXML");
If (~XMLSigner.SetCertificate(CERT_SN))
    ErrorArr.Add(XMLSigner.GetLastError());
    Return "";
End If
'Подписать XML-документ — возвращает подписанный XML или пустую строку'
ResultXML = XMLSigner.Sign(XMLText);
If (ResultXML == "")
    ErrorArr.Add(XMLSigner.GetLastError());
    Return "";
End If
Return ResultXML;
```

Создание объекта с сертификатом в конструкторе в системе EME.WMS:

```EME-L
objDSig = Object("DSigXML", "123ABCDEF");
LastError = objDSig.GetLastError();
If (LastError != "")
    ErrorArr.Add(LastError);
End If
```

#### См. также

- [Класс Archive](./Archive.md) — бинарная сериализация данных для хранения, передачи и восстановления.
- [Класс File](./File.md) — доступ к файлам на диске.
